/**
 * Pagina principal
 */
const Home = () => (
  <section aria-describedby="desc">
    <p id="desc">
      React-router te permite gestionar rutas privadas mediante el uso de
      distintos métodos. También necesitarás almancenar la información de los
      usuarios en algún sitio.
    </p>
  </section>
);

export default Home;
